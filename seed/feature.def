#
# 내부 상태 素生열에서 CRF의 素生열을 추출하기 위한
# 템플릿을 정의하는 파일입니다.
# 각 행은 한 템플릿에 대응합니다.
# UNIGRAM으로 시작하는 것은 UNIGRAM용 템플릿이고
# BIGRAM으로 시작하는 것은 연접(連接)용 템플릿입니다.

# %F[0..N] Unigram 문맥

# %F[n] : UNIGRAM의 n 번째 소성(素性)으로 전개됩니다.
# POS Unigram
UNIGRAM U1:%F[0]
